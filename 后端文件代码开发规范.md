# AI 数字人视频网站后端文件代码开发规范

本文档用于约束 `huashuohouduan` 后端开发。项目后端技术栈为：

- JDK 17
- Spring Boot 3.4.3
- Spring AI 1.1.0
- MySQL 8.0
- Redis
- RabbitMQ 或 Redis Stream
- MyBatis / MyBatis-Plus

本项目以 **MySQL 8.0** 为生产与联调主库；脚本与表结构须与之一致。本地可选用与 MySQL 方言兼容的嵌入式库做快速演示（如 H2 MySQL 模式），上线仍以 MySQL 8.0 为准。

---

## 1. 协作与文件修改规范

1. 后端按业务域拆包，单一职责；文件与包名须体现功能语义，例如 `TaskController`、`AssetService`、`TaskMapper`。（**当前产品为全局工作台，不提供 `ProjectController` / `project` 业务包**。）
2. 修改同一 `Controller`、`Service`、`Mapper`、`Entity` 及关键配置前，须在任务或评审中对齐影响范围（接口、数据、异步任务）。
3. 公共核心代码（`common/*`、启动类、统一响应、异常、链路追踪等）变更须在任务中说明动机与回归方式。
4. 禁止在未经协调的情况下，多人并发修改同一关键文件。

---

## 2. 后端目录结构规范

后端推荐目录如下：

```text
src/main/java/com/huashuo/
├── HuashuoApplication.java
├── common/
│   ├── config/
│   ├── exception/
│   ├── response/
│   ├── trace/
│   └── util/
├── upload/
├── video/
├── script/
├── voice/
├── avatar/
├── render/
├── publish/
├── asset/
├── task/
└── workflow/
```

数据库脚本放在 **模块根目录的 `sql/`** 下（与 `src/`、`pom.xml` 同级），例如：

```text
huashuohouduan/
├── sql/
│   └── schema.sql    # 当前唯一维护：DDL + 种子数据；Maven 构建时复制到 classpath，供 spring.sql.init 执行
├── pom.xml
└── src/
```

业务包划分说明：

- `upload` 可作为第一阶段独立模块保留。
- 上传文件若需被后续流程复用，上传成功后须同步写入 `asset`。
- `storyboard` 不作为一级业务包，归入 `script/storyboard` 或 `script` 下。

每个业务模块内部推荐结构：

```text
task/
├── controller/
│   └── TaskController.java
├── service/
│   ├── TaskService.java
│   └── impl/
│       └── TaskServiceImpl.java
├── mapper/
│   └── TaskMapper.java
├── entity/
│   └── TaskEntity.java
├── dto/
│   └── CreateTaskRequest.java
├── vo/
│   └── TaskItem.java
├── enums/
│   ├── TaskStatusCode.java
│   └── TaskTypeCode.java
├── client/
└── job/
```

要求：

1. Controller 放在 `controller` 包。
2. Service 接口放在 `service` 包。
3. Service 实现放在 `service/impl` 包。
4. Mapper 放在 `mapper` 包。
5. Entity 放在 `entity` 包。
6. DTO 放在 `dto` 包。
7. VO 放在 `vo` 包。
8. 枚举放在 `enums` 包。
9. 第三方调用放在 `client` 包。
10. 异步消费者、定时任务、任务执行器放在 `job` 或 `executor` 包。

---

## 3. 文件命名规范

Java 类名使用 PascalCase，优先使用“业务域 + 功能”的命名方式。

### 3.1 推荐命名

```text
TaskController.java
TaskService.java
TaskServiceImpl.java
TaskMapper.java
TaskEntity.java

AssetController.java
AssetService.java
AssetServiceImpl.java
AssetMapper.java
AssetEntity.java

VideoSourceController.java
ScriptRewriteController.java
TtsController.java
AvatarController.java
RenderController.java
PublishPackController.java
WorkflowClient.java
```

### 3.2 禁止命名

```text
UserEntity.java
CommonUtil.java
TestController.java
DemoService.java
TempMapper.java
```

禁止原因：

1. 过泛命名无法体现业务边界。
2. `Test`、`Demo`、`Temp` 文件容易被误提交到正式代码。

---

## 4. 分层代码规范

每个业务模块按以下层次开发：

| 层级 | 职责 |
| --- | --- |
| `controller` | 接收 HTTP 请求，做参数校验和响应封装 |
| `service` | 定义业务能力，只写接口，不写实现逻辑 |
| `service/impl` | 写业务实现，编排流程、事务、状态变化 |
| `mapper` | 数据库访问，只写 SQL 或 MyBatis-Plus 查询 |
| `entity` | 数据库表映射 |
| `dto` | 前端请求参数 |
| `vo` | 前端响应数据 |
| `enums` | 业务枚举常量 |
| `client` | 第三方 API、Dify、大模型、TTS、视频合成服务调用 |
| `job` / `executor` | 异步任务消费者、任务执行器、定时任务 |
| `handler` / `strategy` | 复杂策略、不同任务类型处理器 |

要求：

1. Controller 不直接调用 Mapper。
2. Controller 不写业务逻辑。
3. Service 只定义能力，不写实现逻辑。
4. 业务实现默认写在 ServiceImpl。
5. ServiceImpl 负责编排流程，不应无限堆积复杂逻辑。
6. 复杂策略、第三方调用、任务执行可拆到 `client`、`job`、`executor`、`handler` 或 `strategy`。
7. Mapper 不写业务逻辑。
8. 数据库写操作必须在 ServiceImpl 层控制事务。
9. 所有异常统一转换为标准错误响应。
10. 生成类任务必须接入 task 模块。

---

## 5. 中文注释规范

后端代码必须在关键业务逻辑处添加中文注释。

要求：

1. 注释说明“为什么这样做”或“这段逻辑保护什么业务规则”。
2. Controller、ServiceImpl、上传、任务状态流转、第三方调用、异常处理等关键逻辑必须有必要注释。
3. 简单 getter、setter、字段赋值、无业务含义的样板代码不需要注释。
4. 注释必须和代码保持一致，修改逻辑时同步修改注释。
5. 禁止无意义注释，例如“创建变量”“调用方法”“返回结果”。

---

## 6. REST API 接口规范

统一接口前缀：

```text
/api/v1
```

接口路径使用小写中划线，资源使用复数或明确业务名。

### 6.1 推荐接口路径

```text
POST   /api/v1/uploads
GET    /api/v1/uploads

POST   /api/v1/video-sources/parse
POST   /api/v1/scripts/analyze
POST   /api/v1/scripts/rewrite
POST   /api/v1/scripts/storyboard

POST   /api/v1/voices/tts
POST   /api/v1/avatars/generate
POST   /api/v1/render-tasks
POST   /api/v1/publish-packs/generate

GET    /api/v1/tasks
GET    /api/v1/tasks/{taskId}
POST   /api/v1/tasks/{taskId}/retry

GET    /api/v1/assets
GET    /api/v1/assets/{assetId}
DELETE /api/v1/assets/{assetId}
```

### 6.2 HTTP 方法约定

1. `GET`：查询，不改变数据。
2. `POST`：创建数据、提交生成任务、触发 AI 流程。
3. `PUT`：完整更新。
4. `PATCH`：局部更新状态或配置。
5. `DELETE`：删除或软删除。

---

## 7. 统一响应格式

所有接口返回统一结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "traceId": "20260428135100001"
}
```

字段说明：

1. `code`：业务状态码，`0` 表示成功。
2. `message`：给前端展示或调试的简短说明。
3. `data`：实际响应数据。
4. `traceId`：一次请求的追踪 ID，便于日志排查。

分页响应统一：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [],
    "pageNo": 1,
    "pageSize": 20,
    "total": 0
  },
  "traceId": "20260428135100001"
}
```

---

## 8. 错误码规范

| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 40000 | 请求参数错误 |
| 40100 | 未登录或登录过期 |
| 40300 | 无权限 |
| 40400 | 数据不存在 |
| 40900 | 状态冲突，不能重复提交 |
| 50000 | 系统异常 |
| 50100 | 第三方服务异常 |
| 50200 | AI 输出格式异常 |
| 50300 | 任务执行失败 |

要求：

1. 后端必须把第三方异常转换为项目错误码。
2. 不允许把原始异常堆栈直接返回给前端。
3. 日志中必须记录 traceId，便于排查。

---

## 9. 请求参数规范

请求 DTO 类名必须体现业务动作和数据用途。

推荐命名：

```text
UploadFileRequest
CreateTaskRequest
ParseVideoSourceRequest
RewriteScriptRequest
GenerateStoryboardRequest
GenerateTtsRequest
GenerateAvatarRequest
RenderVideoRequest
GeneratePublishPackRequest
```

参数要求：

1. 必填字段使用校验注解，例如 `@NotBlank`、`@NotNull`、`@Size`。
2. 项目内数据库主键统一使用 `Long`。
3. 对外返回字段可使用 `projectId`（**可选 / 可空，兼容**）、`taskId`、`assetId` 等，类型统一为 `Long`。
4. 时间字段统一使用 `LocalDateTime`，接口返回 ISO 格式字符串。
5. 金额、成本、耗时字段禁止使用 `double`，使用 `BigDecimal` 或整数毫秒。
6. AI 生成参数必须保存原始输入、Prompt 版本、模型名称和输出结果。

---

## 10. 核心数据对象约定

MVP 阶段核心对象：

| 对象 | 说明 |
| --- | --- |
| `project` | **库表可保留**；不作为当前产品主对象，**无**对应项目管理 REST |
| `uploaded_file` | 第一阶段上传流水记录 |
| `video_source` | 原始短视频链接、平台、标题、基础数据 |
| `script_version` | 文案和分镜脚本版本 |
| `voice_profile` | 声音配置 |
| `avatar_profile` | 数字人形象配置 |
| `render_config` | 字幕、BGM、音量、比例等视频配置 |
| `publish_pack` | 标题、简介、话题、封面结果 |
| `asset` | 图片、音频、视频、封面、文案等统一资产 |
| `task` | 所有解析、生成、合成任务 |

生成结果绑定规则：

1. **`projectId` 可选**：任务与资产均可携带可空的 `project_id`，全局工作台**不要求**必须先有项目。
2. 所有生成类任务必须绑定 **`taskId`（任务主键）**。
3. 最终可复用、可预览、可下载的结果必须进入 `asset`。
4. 中间 JSON 结果可以先保存在 `task.output_json`、`script_version` 或对应业务表。
5. 并不是所有中间结果都必须立即生成 `assetId`。
6. 最终进入资产中心的结果必须绑定 **`taskId`（或可追溯至任务）**、`assetId`、`sourceType`、`createdAt`；**`projectId` 不强制**。

---

## 11. 异步任务规范

视频解析、文案分析、文案改写、分镜生成、TTS、图片生成、视频合成、封面生成都按任务处理。

耗时超过 3 秒的接口不得同步等待完成。

任务状态统一使用：

```text
QUEUED      排队中
RUNNING     处理中
SUCCESS     成功
FAILED      失败
RETRYABLE   可重试
```

任务类型推荐：

```text
VIDEO_PARSE
SCRIPT_ANALYZE
SCRIPT_REWRITE
STORYBOARD_GENERATE
TTS_GENERATE
AVATAR_GENERATE
VIDEO_RENDER
PUBLISH_PACK_GENERATE
COVER_GENERATE
```

创建任务接口返回：

```json
{
  "taskId": 10001,
  "projectId": null,
  "taskType": "VIDEO_RENDER",
  "status": "QUEUED"
}
```

任务要求：

1. 任务必须保存输入参数快照。
2. 任务必须保存输出结果或失败原因。
3. 失败任务必须记录 `errorCode`、`errorMessage`、`retryCount`。
4. 视频合成任务必须支持查询进度。
5. 同一个任务不能被多个消费者重复执行，必须有锁或状态抢占机制。
6. 当前阶段可以先使用数据库状态流转，后续再接 Redis / RabbitMQ。
7. 所有业务模块提交生成任务时，必须调用 task 模块的统一方法，不得私自写 task 表。

---

## 12. 资产中心规范

所有最终可复用、可预览、可下载的结果必须进入资产中心。

资产类型统一使用：

```text
TEXT
IMAGE
AUDIO
VIDEO
COVER
JSON
```

资产字段至少包含：

1. `assetId`
2. `projectId`
3. `taskId`
4. `assetType`
5. `fileName`
6. `filePath`
7. `previewUrl`
8. `mimeType`
9. `fileSize`
10. `sourceType`
11. `createdBy`
12. `createdAt`

sourceType 推荐值：

```text
USER_UPLOAD
AI_GENERATED
SYSTEM_MOCK
MANUAL_CREATED
```

要求：

1. 上传文件如果需要后续复用，必须生成 asset。
2. AI 生成的图片、音频、视频、封面必须生成 asset。
3. 文案类结果可以先进入 `script_version`，需要复用或预览时再同步生成 TEXT 类型 asset。
4. 模块不得私自保存可复用结果而不进入资产中心。
5. MVP 阶段优先使用本地存储；正式上线前再切换 MinIO、OSS、S3 或其他云存储。

---

## 13. uploaded_file 与 asset 的关系

`uploaded_file` 是第一阶段文件上传演示和上传流水表。

`asset` 是长期统一资产中心表。

规则：

1. `uploaded_file` 记录用户原始上传行为。
2. `asset` 记录系统内可复用、可预览、可下载的资产。
3. 上传成功后，如果文件需要被后续流程使用，必须同步创建 `asset` 记录。
4. 后续业务页面应优先使用 `asset` 查询可用素材。
5. 不允许出现同一文件只在 `uploaded_file` 中存在、但后续生成流程无法通过 `asset` 复用的情况。
6. 若后续 asset 能完全覆盖上传流水需求，可以逐步弱化 `uploaded_file`。

---

## 14. AI 输出规范

AI 输出必须优先使用结构化 JSON，禁止只返回无法解析的自然语言。

所有 AI 流程必须记录：

1. 业务类型。
2. Prompt 模板版本。
3. 模型名称。
4. 请求参数。
5. 原始响应。
6. 解析后的结构化结果。
7. 失败原因。

文案分析、文案改写、分镜脚本、标题封面等接口必须在后端校验 JSON 结构。

当 AI 输出格式不合格时：

1. 返回错误码 `50200`。
2. 或将任务状态置为 `RETRYABLE`。
3. 必须记录失败原因，便于重试和排查。

---

## 15. 数据库与迁移规范

本项目开发与联调数据库以 **MySQL 8.0** 为准。

表名使用小写下划线：

```text
project
uploaded_file
video_source
script_version
voice_profile
avatar_profile
render_config
publish_pack
asset
task
```

字段名使用小写下划线：

```text
project_id
task_id
asset_type
created_at
updated_at
deleted
```

SQL 文件按版本和功能命名：

```text
V001__create_project_table.sql
V002__create_uploaded_file_table.sql
V003__create_task_table.sql
V004__create_asset_table.sql
V005__create_script_version_table.sql
```

如果当前阶段未接入 Flyway 或 Liquibase，统一在 **`huashuohouduan/sql/`** 下维护脚本（当前为单一文件）：

```text
sql/schema.sql
```

说明：`schema.sql` 由 Maven `resources` 复制到运行时 `classpath:schema.sql`，与 `application.yml` / `application-dev.yml` 中 `spring.sql.init.schema-locations` 对应；**不要**再放一份到 `src/main/resources/`，避免双源不一致。

但要求：

1. `sql/schema.sql` 必须以 MySQL 8.0 为基准编写，并在目标版本上验证。
2. 若本地使用与 MySQL 方言兼容的嵌入式库做演示，脚本须能在该环境下执行，且与 MySQL 8.0 行为冲突时以 MySQL 为准并在注释或任务中说明。
3. JSON 字段按 MySQL 8.0 语法设计。
4. 索引、字段类型、默认值以 MySQL 8.0 为准。
5. 后续接入迁移工具时，按 `V001__xxx.sql` 拆分并迁入 `sql/`（或工具约定目录）。

---

## 16. 通用字段规范

所有业务表字段必须遵守：

1. 主键字段命名为 `{业务名}_id`，例如 `project_id`、`file_id`、`task_id`、`asset_id`。
2. 外键引用字段必须与被引用主键同名，例如统一使用 `project_id`。
3. 项目内主键统一使用 `bigint`。
4. 状态字段统一使用 `varchar(30)`，枚举值使用大写英文。
5. 时间字段统一使用 `created_at`、`updated_at`，类型为 `datetime`。
6. 软删除字段统一使用 `deleted`，MySQL 类型为 `tinyint(1)`。
7. 文件大小使用 `bigint` 存储字节数，不使用字符串。
8. 成本金额使用 `decimal(12,4)`，禁止使用 `float` 或 `double`。
9. 大文本、AI 原始输入输出、JSON 快照使用 `json` 或 `longtext`，必须说明结构。
10. 所有表必须有主键、创建时间、更新时间、软删除字段。
11. 业务 ID 字段必须建立索引。
12. 任务表必须对 `status`、`task_type`、`project_id` 建索引。
13. 资产表必须对 `project_id`、`task_id`、`asset_type` 建索引。

---

## 17. 第一阶段基础表设计

### 17.1 project：兼容表（**无项目管理 REST**）

> **2026-05 起**：产品为 **全局工作台**，后端**不提供**项目 CRUD。下表描述的是 `schema.sql` 中**仍可能保留**的 `project` 表，用于种子数据或历史外键；**新业务不得假设用户已创建项目**。

| 字段名 | 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | `bigint` | 是 | 主键 | 自增主键 |
| `project_name` | `varchar(80)` | 是 | 否 | 名称 |
| `description` | `varchar(500)` | 否 | 否 | 描述 |
| `status` | `varchar(30)` | 是 | 是 | 如 `DRAFT`、`MAKING`、`DONE` |
| `created_at` | `datetime` | 是 | 否 | 创建时间 |
| `updated_at` | `datetime` | 是 | 否 | 更新时间 |
| `deleted` | `tinyint(1)` | 是 | 是 | 软删除 |

字段规范：

1. 该表由种子或运维维护；**禁止**在应用代码中新增「项目管理」接口依赖。
2. 任务、资产、脚本等表的 `project_id` **可空**，与 `schema.sql` 一致。

### 17.2 uploaded_file：文件上传流水表

| 字段名 | 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- |
| `file_id` | `bigint` | 是 | 主键 | 文件 ID，自增 |
| `project_id` | `bigint` | **否** | 是 | **可选**；兼容字段，可空 |
| `owner_user_id` | `bigint` | **否** | 是 | **上传归属**；与全局列表可见性相关（见实现对齐） |
| `original_file_name` | `varchar(255)` | 是 | 否 | 用户上传时的原始文件名 |
| `stored_file_name` | `varchar(255)` | 是 | 否 | 服务端生成的存储文件名 |
| `file_path` | `varchar(1000)` | 是 | 否 | 服务端本地存储路径 |
| `preview_url` | `varchar(1000)` | 是 | 否 | 前端可访问的预览地址 |
| `mime_type` | `varchar(120)` | 否 | 否 | 文件 MIME 类型 |
| `file_size` | `bigint` | 是 | 否 | 文件大小，单位字节 |
| `created_at` | `datetime` | 是 | 否 | 创建时间 |
| `updated_at` | `datetime` | 是 | 否 | 更新时间 |
| `deleted` | `tinyint(1)` | 是 | 是 | 软删除标识 |

字段规范：

1. **`project_id` 不强制**；上传记录应写入 **`owner_user_id`（若已登录）** 以支持全局列表收敛。
2. `stored_file_name` 由后端生成，禁止直接使用用户原始文件名保存。
3. `preview_url` 只返回访问地址，不暴露密钥。
4. 上传成功后，如文件需要复用，必须同步创建 asset。
5. 上传目录、文件大小限制、预览前缀必须配置化，禁止硬编码到 Controller。

### 17.3 task：任务中心基础表

| 字段名 | 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- |
| `task_id` | `bigint` | 是 | 主键 | 任务 ID，自增 |
| `project_id` | `bigint` | **否** | 是 | **可选**；兼容字段，可空 |
| `task_type` | `varchar(50)` | 是 | 是 | 任务类型 |
| `status` | `varchar(30)` | 是 | 是 | 任务状态 |
| `input_json` | `json` | 否 | 否 | 输入参数快照 |
| `output_json` | `json` | 否 | 否 | 输出结果快照 |
| `error_code` | `varchar(50)` | 否 | 否 | 失败错误码 |
| `error_message` | `text` | 否 | 否 | 失败原因 |
| `retry_count` | `int` | 是 | 否 | 重试次数 |
| `progress` | `int` | 是 | 否 | 任务进度，0-100 |
| `trace_id` | `varchar(100)` | 否 | 是 | 链路追踪 ID |
| `created_at` | `datetime` | 是 | 否 | 创建时间 |
| `updated_at` | `datetime` | 是 | 否 | 更新时间 |
| `deleted` | `tinyint(1)` | 是 | 是 | 软删除标识 |

### 17.4 asset：资产中心基础表

| 字段名 | 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- |
| `asset_id` | `bigint` | 是 | 主键 | 资产 ID，自增 |
| `project_id` | `bigint` | 是 | 是 | 所属项目 ID |
| `task_id` | `bigint` | 否 | 是 | 来源任务 ID |
| `asset_type` | `varchar(30)` | 是 | 是 | 资产类型 |
| `file_name` | `varchar(255)` | 是 | 否 | 文件名 |
| `file_path` | `varchar(1000)` | 否 | 否 | 文件存储路径 |
| `preview_url` | `varchar(1000)` | 否 | 否 | 预览地址 |
| `mime_type` | `varchar(120)` | 否 | 否 | MIME 类型 |
| `file_size` | `bigint` | 否 | 否 | 文件大小 |
| `source_type` | `varchar(50)` | 是 | 是 | 来源类型 |
| `metadata_json` | `json` | 否 | 否 | 扩展元数据 |
| `created_by` | `varchar(100)` | 否 | 否 | 创建人 |
| `created_at` | `datetime` | 是 | 否 | 创建时间 |
| `updated_at` | `datetime` | 是 | 否 | 更新时间 |
| `deleted` | `tinyint(1)` | 是 | 是 | 软删除标识 |

---

## 18. 日志规范

日志必须能定位一次请求、一次任务和一次第三方调用。

必须记录：

1. `traceId`
2. `taskId`
3. `projectId`
4. 接口路径
5. 请求耗时
6. 第三方服务名称
7. 失败原因

禁止记录：

1. API Key、Token、密钥。
2. 用户隐私数据。
3. 大体积图片、音频、视频的完整内容。

---

## 19. 配置规范

配置按环境和功能拆分：

```text
application.yml
application-local.yml
application-dev.yml
application-prod.yml
application-redis-local.yml
application-mysql-local.yml
```

要求：

1. 本地与联调环境的数据库连接须与 `application*.yml` 一致；生产以 MySQL 8.0 为准。
2. 数据库连接、账号、密码必须配置化。
3. 密钥只通过环境变量或本地未提交配置读取。
4. 禁止把真实 API Key 写入 Git。
5. 第三方服务地址、超时时间、重试次数必须配置化。
6. 本地个人配置不得提交；需要共享的环境配置按环境或功能命名。

---

## 20. 提交前检查清单

每次提交前必须确认：

1. 文件名使用功能/业务语义前缀，不使用姓名缩写。
2. 没有未经同步修改他人正在维护的关键文件。
3. 新接口已写清楚路径、请求、响应和错误码。
4. 生成类流程已接入任务中心。
5. 最终可复用结果已进入资产中心。
6. 第三方异常已转换为统一错误码。
7. 没有提交密钥、Token、本地临时文件。
8. 本地或 CI 所配置的数据源已启动且可连通。
9. 本地启动、编译或单元测试通过。
10. 接口返回结构符合 ApiResponse。

---

## 21. 联调约定

后端接口完成后，负责人必须提供：

1. 接口路径。
2. 请求示例。
3. 响应示例。
4. 错误码。
5. 是否异步。
6. 如果异步，提供 `taskId` 查询方式。
7. 资产结果是否已进入资产中心。
8. 失败场景如何返回。
9. 是否需要前端轮询任务状态。

前后端联调以接口文档为准。

接口字段一旦被前端使用，后端修改字段名、字段类型或状态枚举前必须提前同步。
