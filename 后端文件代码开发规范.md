# AI 数字人视频网站后端文件代码开发规范

本文档用于约束 `huashuohouduan` 后端开发。项目后端技术栈为 JDK 17、Spring Boot 3.4.3、Spring AI 1.1.0、MySQL 8.0、Redis、RabbitMQ 或 Redis Stream、MyBatis / MyBatis-Plus。

## 1. 开发人员与文件归属

后端由费文轩、黄宝泉共同开发，但同一时间不能编辑同一个文件。文件命名以功能和业务语义为前缀，不再使用姓名缩写作为前缀；负责人通过业务域边界、飞书任务卡和代码评审记录确认。

| 人员 | 主要后端职责 |
| --- | --- |
| 费文轩 | 项目基础接口、用户工作台、TTS、图片生成、标题封面、前后端联调 |
| 黄宝泉 | 任务中心、资产中心、AI 工作流、视频合成、异步队列、Redis/MQ、部署稳定性 |

协作规则：
1. **按业务域拆包**：`project / task / asset / workflow / render / script / voice / avatar / publish / video` 等，禁止按人员拆包。
2. **按功能命名文件**：业务域内新增的 `Controller/Service/Mapper/DTO/VO/Client/Job/SQL` 等文件名必须体现功能，不使用姓名缩写，例如 `ProjectController`、`TaskService`、`AssetMapper`、`RenderJob`。
3. **每个文件唯一维护人**：谁创建的业务文件，谁维护；另一人不得直接修改该文件。确需修改时，先在飞书任务卡说明原因、修改范围、接口影响并得到确认。
4. **公共/核心抽象单负责人**：`common/*`、启动类、全局配置、统一异常/响应、链路追踪等公共核心文件也按功能命名，例如 `ApiResponse`、`BusinessException`、`TraceIdFilter`；必须在任务卡里明确唯一负责人。
5. **禁止双人同时改同一业务域关键文件**：同一个功能不允许两个人同时改同一个 Controller、Service、Mapper、实体或关键配置文件。

## 2. 业务域负责人边界（精简版）

后端按业务域划分负责人，避免跨域修改同一文件：

| 业务域 | 负责人 | 边界说明 |
| --- | --- | --- |
| `project`、`voice`、`avatar`、`publish`、`upload` | 费文轩 | 项目管理、TTS/声音、形象、标题封面、上传与联调 |
| `task`、`asset`、`workflow`、`render`、`video`、`script` | 黄宝泉 | 任务中心、资产中心、AI 编排、视频合成链路、队列、重试、Redis/MQ |

## 3. 后端目录建议

推荐按业务域组织包，不按人员单独拆包。文件名体现功能，负责人记录在任务卡和业务域边界中。

```text
src/main/java/com/huashuo/
├── HuashuoApplication.java
├── common/
│   ├── config/
│   ├── exception/
│   ├── response/
│   └── util/
├── project/
├── video/
├── script/
├── voice/
├── avatar/
├── render/
├── publish/
├── asset/
├── task/
└── workflow/
```

职责边界：
1. `project`：项目列表、项目详情、项目内资源聚合，默认费文轩负责。
2. `video`：短视频链接解析、原始文案提取，默认黄宝泉负责，费文轩协作联调。
3. `script`：文案分析、文案改写、分镜脚本改写，默认黄宝泉负责结构化输出，费文轩协作页面接口。
4. `voice`：声音选择、试听、TTS 音频生成，默认费文轩负责。
5. `avatar`：形象上传、形象生成、参考图处理，默认费文轩负责。
6. `render`：数字人视频合成、字幕、背景音乐、音量、比例配置，默认黄宝泉负责。
7. `publish`：标题、简介、话题、封面生成，默认费文轩负责。
8. `asset`：图片、音频、视频、文案、封面统一资产中心，默认黄宝泉负责。
9. `task`：任务状态机、队列、重试、日志、并发控制，默认黄宝泉负责。
10. `workflow`：Dify、大模型、AI 编排、JSON Schema 输出，默认黄宝泉负责。

## 4. 文件命名规范

Java 类名使用 PascalCase，优先使用“业务域 + 功能”的命名方式，不使用姓名缩写作为前缀。配置文件、SQL、脚本也按功能命名。

### 4.1 功能命名示例

```text
ProjectController.java
ProjectService.java
ProjectServiceImpl.java
ProjectMapper.java
ProjectEntity.java
TtsController.java
AvatarController.java
PublishController.java
V001__create_project_table.sql
application-local.yml
```

### 4.2 任务与工作流命名示例

```text
TaskController.java
TaskService.java
TaskServiceImpl.java
TaskMapper.java
TaskEntity.java
AssetController.java
RenderController.java
WorkflowClient.java
V002__create_task_table.sql
application-redis-local.yml
```

### 4.3 禁止命名

```text
FwxProjectController.java
HbqTaskService.java
UserEntity.java
CommonUtil.java
```

原因：姓名前缀会污染业务命名；过泛的 `UserEntity`、`CommonUtil` 无法体现业务边界，容易造成多人同时编辑。

## 5. 分层代码规范

每个业务模块按以下层次开发：

```text
controller  接收 HTTP 请求，只做参数校验和响应封装
service     编排业务流程，处理事务和状态变化
mapper      数据库访问，只写 SQL 或 MyBatis-Plus 查询
entity      数据库表映射
dto         前端请求参数
vo          前端响应数据
client      第三方 API、Dify、大模型、TTS、视频合成服务调用
job         异步任务消费者或定时任务
```

要求：
1. Controller 不直接调用 Mapper。
2. Mapper 不写业务逻辑。
3. 第三方接口调用必须封装在 `client` 层。
4. 生成类任务必须通过 `task` 模块落库，不能直接同步阻塞等待。
5. 数据库写操作必须在 Service 层控制事务。
6. 所有异常统一转换为标准错误响应。

## 6. 中文注释规范

后端代码必须在关键业务逻辑处添加中文注释，帮助两个人快速理解调用链和边界。

要求：
1. 注释使用中文，说明“为什么这样做”或“这段逻辑保护什么业务规则”。
2. Controller、Service、上传、任务、第三方调用、异常处理、状态流转等关键逻辑必须有必要注释。
3. 简单 getter、setter、字段赋值、无业务含义的样板代码不需要注释。
4. 注释必须和代码保持一致，修改逻辑时同步修改注释。
5. 禁止写无意义注释，例如“创建变量”“调用方法”“返回结果”。

## 7. REST API 接口规范

统一前缀：

```text
/api/v1
```

接口路径使用小写中划线，资源使用复数或明确业务名。

```text
GET    /api/v1/projects
POST   /api/v1/projects
GET    /api/v1/projects/{projectId}
POST   /api/v1/video-sources/parse
POST   /api/v1/scripts/rewrite
POST   /api/v1/voices/tts
POST   /api/v1/avatars/generate
POST   /api/v1/render-tasks
GET    /api/v1/tasks/{taskId}
GET    /api/v1/assets
POST   /api/v1/publish-packs/generate
```

HTTP 方法约定：
1. `GET`：查询，不改变数据。
2. `POST`：创建数据、提交生成任务、触发 AI 流程。
3. `PUT`：完整更新。
4. `PATCH`：局部更新状态或配置。
5. `DELETE`：删除或软删除。

## 8. 统一响应格式

所有接口返回统一结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "traceId": "20260428135100001"
}
```

字段说明：
1. `code`：业务状态码，`0` 表示成功。
2. `message`：给前端展示或调试的简短说明。
3. `data`：实际响应数据。
4. `traceId`：一次请求的追踪 ID，便于日志排查。

分页响应统一：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [],
    "pageNo": 1,
    "pageSize": 20,
    "total": 0
  },
  "traceId": "20260428135100001"
}
```

## 9. 错误码规范

| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 40000 | 请求参数错误 |
| 40100 | 未登录或登录过期 |
| 40300 | 无权限 |
| 40400 | 数据不存在 |
| 40900 | 状态冲突，不能重复提交 |
| 50000 | 系统异常 |
| 50100 | 第三方服务异常 |
| 50200 | AI 输出格式异常 |
| 50300 | 任务执行失败 |

后端必须把第三方异常转换为项目错误码，不允许把原始异常堆栈直接返回给前端。

## 10. 请求参数规范

请求 DTO 类名必须体现业务动作和数据用途：

```text
CreateProjectRequest
GenerateTtsRequest
CreateTaskRequest
RenderVideoRequest
```

参数要求：
1. 必填字段使用校验注解，例如 `@NotBlank`、`@NotNull`、`@Size`。
2. ID 字段统一使用 `Long` 或 `String`，同一张表内保持一致。
3. 时间字段统一使用 `LocalDateTime`，接口返回 ISO 格式字符串。
4. 金额、成本、耗时字段禁止使用 `double`，使用 `BigDecimal` 或整数毫秒。
5. AI 生成参数必须保存原始输入、Prompt 版本、模型名称和输出结果。

## 11. 核心数据对象约定

MVP 阶段至少包含以下对象：

| 对象 | 说明 | 默认负责人 |
| --- | --- | --- |
| `project` | 一个视频制作项目 | 费文轩 |
| `video_source` | 原始短视频链接、平台、标题、基础数据 | 黄宝泉 |
| `script_version` | 文案和分镜脚本版本 | 黄宝泉 |
| `voice_profile` | 声音配置 | 费文轩 |
| `avatar_profile` | 数字人形象配置 | 费文轩 |
| `render_config` | 字幕、BGM、音量、比例等视频配置 | 黄宝泉 |
| `publish_pack` | 标题、简介、话题、封面结果 | 费文轩 |
| `asset` | 图片、音频、视频、封面、文案等统一资产 | 黄宝泉 |
| `task` | 所有解析、生成、合成任务 | 黄宝泉 |

所有生成结果必须绑定：
1. `projectId`
2. `taskId`
3. `assetId`
4. `inputSnapshot`
5. `outputSnapshot`
6. `createdBy`
7. `createdAt`

## 12. 异步任务规范

视频解析、文案改写、TTS、图片生成、视频合成、封面生成都按任务处理。耗时超过 3 秒的接口不得同步等待完成。

任务状态至少包括：

```text
QUEUED      排队中
RUNNING     处理中
SUCCESS     成功
FAILED      失败
RETRYABLE   可重试
CANCELLED   已取消
```

创建任务接口返回：

```json
{
  "taskId": "task_10001",
  "projectId": "project_10001",
  "taskType": "RENDER_VIDEO",
  "status": "QUEUED"
}
```

任务要求：
1. 任务必须保存输入参数快照。
2. 任务必须保存输出结果或失败原因。
3. 失败任务必须记录 `errorCode`、`errorMessage`、`retryCount`。
4. 视频合成任务必须支持查询进度。
5. 同一个任务不能被多个消费者重复执行，必须有锁或状态抢占机制。

## 13. 资产中心规范

所有文案、图片、音频、视频、封面都必须进入资产中心。禁止模块私自保存生成结果但不入库。

资产类型：

```text
TEXT
IMAGE
AUDIO
VIDEO
COVER
JSON
```

资产字段至少包含：
1. `assetId`
2. `projectId`
3. `taskId`
4. `assetType`
5. `fileName`
6. `filePath`
7. `previewUrl`
8. `mimeType`
9. `fileSize`
10. `sourceType`
11. `createdBy`
12. `createdAt`

MVP 阶段可以使用本地存储或 MinIO，正式上线前再切换 OSS、S3 或其他云存储。

## 14. AI 输出规范

AI 输出必须优先使用结构化 JSON，禁止只返回无法解析的自然语言。

所有 AI 流程必须记录：
1. 业务类型。
2. Prompt 模板版本。
3. 模型名称。
4. 请求参数。
5. 原始响应。
6. 解析后的结构化结果。
7. 失败原因。

文案分析、文案改写、分镜脚本、标题封面等接口必须在后端校验 JSON 结构，不合格时返回 `50200` 或进入可重试状态。

## 15. 数据库与迁移规范

表名使用小写下划线：

```text
project
video_source
script_version
voice_profile
avatar_profile
render_config
publish_pack
asset
task
```

字段名使用小写下划线：

```text
project_id
task_id
asset_type
created_at
updated_at
deleted
```

SQL 文件按版本和功能命名：

```text
V001__create_project_table.sql
V002__create_task_table.sql
V003__create_asset_table.sql
V004__create_voice_profile_table.sql
```

要求：
1. 所有表必须有主键、创建时间、更新时间、软删除字段。
2. 业务 ID 字段必须建立索引。
3. 任务表必须对 `status`、`task_type`、`project_id` 建索引。
4. 资产表必须对 `project_id`、`task_id`、`asset_type` 建索引。

### 15.1 本周演示表设计

本周演示内容包括项目管理基础接口和文件上传基础能力，演示库先使用 H2，正式库按 MySQL 8.0 建表。对应 SQL 文件为：

```text
src/main/resources/schema.sql
```

#### project：项目管理基础表

| 字段名 | 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | `bigint` | 是 | 主键 | 项目 ID，自增 |
| `project_name` | `varchar(80)` | 是 | 否 | 项目名称 |
| `description` | `varchar(500)` | 否 | 否 | 项目描述 |
| `status` | `varchar(30)` | 是 | 是 | 项目状态，MVP 阶段使用 `DRAFT`、`MAKING`、`DONE` |
| `created_at` | `datetime` | 是 | 否 | 创建时间 |
| `updated_at` | `datetime` | 是 | 否 | 更新时间 |
| `deleted` | `tinyint(1)` | 是 | 是 | 软删除标识，`0` 未删除，`1` 已删除 |

字段规范：
1. `project_name` 不能为空，前后端都限制最多 80 个字符。
2. `status` 不允许前端随意传值，后端负责控制状态流转。
3. 后续所有生成任务、资产、脚本版本都必须绑定 `project_id`。
4. 删除项目时优先软删除，不直接物理删除。

#### uploaded_file：文件上传演示表

| 字段名 | 类型 | 必填 | 索引 | 说明 |
| --- | --- | --- | --- | --- |
| `file_id` | `bigint` | 是 | 主键 | 文件 ID，自增 |
| `project_id` | `bigint` | 是 | 是 | 所属项目 ID |
| `original_file_name` | `varchar(255)` | 是 | 否 | 用户上传时的原始文件名 |
| `stored_file_name` | `varchar(255)` | 是 | 否 | 服务端生成的存储文件名，使用功能前缀，例如 `upload-` |
| `file_path` | `varchar(1000)` | 是 | 否 | 服务端本地存储路径 |
| `preview_url` | `varchar(1000)` | 是 | 否 | 前端可访问的预览地址 |
| `mime_type` | `varchar(120)` | 否 | 否 | 文件 MIME 类型 |
| `file_size` | `bigint` | 是 | 否 | 文件大小，单位字节 |
| `created_at` | `datetime` | 是 | 否 | 创建时间 |
| `updated_at` | `datetime` | 是 | 否 | 更新时间 |
| `deleted` | `tinyint(1)` | 是 | 是 | 软删除标识 |

字段规范：
1. 上传文件必须先绑定有效 `project_id`。
2. `stored_file_name` 由后端生成，禁止直接使用用户原始文件名保存。
3. `preview_url` 只返回访问地址，不暴露密钥。
4. 本表是第一周文件上传演示表，后续接入统一资产中心后，图片、音频、视频、文案、封面必须进入 `asset` 表。
5. 上传目录、文件大小限制、预览前缀必须配置化，禁止硬编码到 Controller。

### 15.2 通用字段规范

所有业务表字段必须遵守：
1. 主键字段命名为 `{业务名}_id`，例如 `project_id`、`file_id`、`task_id`、`asset_id`。
2. 外键引用字段必须与被引用主键同名，例如统一使用 `project_id`。
3. 状态字段统一使用 `varchar(30)`，枚举值使用大写英文。
4. 时间字段统一使用 `created_at`、`updated_at`，类型为 `datetime`。
5. 软删除字段统一使用 `deleted`，MySQL 类型为 `tinyint(1)`。
6. 文件大小使用 `bigint` 存储字节数，不使用字符串。
7. 成本金额使用 `decimal(12,4)`，禁止使用 `float` 或 `double`。
8. 大文本、AI 原始输入输出、JSON 快照使用 `json` 或 `longtext`，必须说明结构。

## 16. 日志规范

日志必须能定位一次请求、一次任务和一次第三方调用。

必须记录：
1. `traceId`
2. `taskId`
3. `projectId`
4. 接口路径
5. 请求耗时
6. 第三方服务名称
7. 失败原因

禁止记录：
1. API Key、Token、密钥。
2. 用户隐私数据。
3. 大体积图片、音频、视频的完整内容。

## 17. 配置规范

配置按环境拆分：

```text
application.yml
application-local.yml
application-dev.yml
application-prod.yml
application-local.yml
application-redis-local.yml
```

要求：
1. 密钥只通过环境变量或本地未提交配置读取。
2. 禁止把真实 API Key 写入 Git。
3. 第三方服务地址、超时时间、重试次数必须配置化。
4. 本地个人配置不得提交；需要共享的环境配置按环境或功能命名。

## 18. 提交前检查清单

每次提交前必须确认：
1. 文件名使用功能/业务语义前缀，不使用姓名缩写。
2. 没有修改对方负责文件。
3. 新接口已写清楚路径、请求、响应和错误码。
4. 生成类流程已接入任务中心和资产中心。
5. 第三方异常已转换为统一错误码。
6. 没有提交密钥、Token、本地临时文件。
7. 本地启动、编译或单元测试通过。

## 19. 联调约定

后端接口完成后，负责人必须提供：
1. 接口路径。
2. 请求示例。
3. 响应示例。
4. 错误码。
5. 是否异步。
6. 如果异步，提供 `taskId` 查询方式。
7. 资产结果是否已进入资产中心。

前后端联调以接口文档为准。接口字段一旦被前端使用，后端修改字段名、字段类型或状态枚举前必须提前同步。
